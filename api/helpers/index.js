const status = require('./errors');
const validEmail = require('./validEmail');
const validName = require('./validName');
const validPassword = require('./validPassword');

module.exports = { status, validEmail, validName, validPassword };